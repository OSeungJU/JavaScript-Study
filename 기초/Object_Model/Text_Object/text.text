값 API
    - 텍스트 노드는 DOM에서 실질적인 데이터가 저장되는 객체이다. 따라서 텍스트 노드에는 값과 관련된 여러 기능들이 있다.
        1. nodevalue
        2. data

조작 API
    - 텍스트 노드가 상속 받은 CharacterData 객체는 문자를 제어할 수 있는 다양한 API를 제공한다.
      아래 조작과 관련된 API 목록들이다.
        1. appendData()
        2. deleteData()
        3. insertData()
        4. replaceData()
        5. substringData()